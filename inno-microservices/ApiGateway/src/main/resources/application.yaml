server:
  port: 8085

spring:
  application:
    name: ApiGateway

security:
  jwt:
    secret-key: ${JWT_SECRET_KEY}

user:
  service:
    url: ${USER_SERVICE_URI:http://localhost:8080}

auth:
  service:
    url: ${AUTH_SERVICE_URI:http://localhost:8081}

order:
  service:
    url: ${ORDER_SERVICE_URI:http://localhost:8082}